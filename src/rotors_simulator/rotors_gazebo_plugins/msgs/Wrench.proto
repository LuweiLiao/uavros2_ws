syntax = "proto2";
package gz_geometry_msgs;

import "gz/msgs/vector3d.proto";

// Wrench includes both force and torque information about a joint.
message Wrench
{
  required gz.msgs.Vector3d force = 1;
  required gz.msgs.Vector3d torque = 2;
}
